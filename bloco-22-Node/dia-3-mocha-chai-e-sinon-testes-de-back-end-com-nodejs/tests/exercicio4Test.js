/* const fs = require('fs');
const { expect } = require('chai');

const escreveExercicio4 = requirerequire('./escrevaArquivo');

describe('Escreve arquivo', () => {
  describe('Quando o arquivo existe', () => {
    describe('a resposta', () => {
      it('é uma string', () => {
        const resposta = escreveExercicio4('arquivo.txt', '#vqv conteúdo');

        expect(resposta).to.be.a('string');
      });

      it('é igual a "ok"', () => {
        const resposta = escrevaArquivo('arquivo.txt', '#vqv conteúdo');
  
        expect(resposta).to.be.equals('ok');
      });
    });
  });
}); */
